import { useContext } from "react";
import { Provider, BlogListMainContext } from "./provider/main";

const BlogList = (props) => {
  return <Provider></Provider>;
};

const mainCtx = useContext(BlogListMainContext);
